\* Negative control: the handoff re-attach bug enabled (Bypass=TRUE).  A SUSPENDED
\* tstate can be dragged back to ATTACHED while the world is stopped -> STWExclusive
\* MUST be violated (the formal counterpart of bug 2 / contract C3).
CONSTANTS
    Hubs = {h1, h2, h3}
    NoHub = NoHub
    External = External
    Bypass = TRUE
    BuggyBlock = FALSE
    MaxStops = 2
SPECIFICATION Spec
INVARIANT TypeOK
INVARIANT STWExclusive
INVARIANT RequesterAttached
