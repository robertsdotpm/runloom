\* GENERATED. Hubs must cover the trace's hub ids; DeleteOnOwner is unused on the
\* trace path (DoDelete is driven directly by the recorded events).
CONSTANTS
    Hubs = {"h1", "h2", "h3", "r1", "r2", "r3"}
    DeleteOnOwner = TRUE
SPECIFICATION TSpec
INVARIANT TypeOK
INVARIANT GilstateContract
INVARIANT GilBindingConsistent
