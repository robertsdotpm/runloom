\* Rescue enabled: a wedged hub is adopted + drained, so all work completes.
SPECIFICATION Spec
CONSTANTS
    Hubs = {h1, h2}
    InitWork = 2
    EnableRescue = TRUE
INVARIANT TypeOK
INVARIANT NoConcurrentDrain
PROPERTY AllDrained
