\* GENERATED.  MaxRuns >= the max Grants-to-any-hub in the trace (each Grant+Release
\* is one run; Arrive needs runs < MaxRuns).  Timers off (the workload has none).
CONSTANTS
    Hubs = {"h1", "h2", "h3"}
    NoHub = NoHub
    Preempt = TRUE
    Barrier = TRUE
    Timers = FALSE
    LogicalClock = TRUE
    MaxClock = 0
    MaxRuns = 6
SPECIFICATION TSpec
INVARIANT TypeOK
INVARIANT MutualExclusion
INVARIANT BatonConsistent
INVARIANT DeterministicGrant
