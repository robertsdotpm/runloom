\* Correct: mn_fini signals idle_cond UNDER idle_lock, so the stop signal can never
\* be lost in the hub's check-then-wait window -> the hub always exits and the
\* pthread_join always completes.
CONSTANTS Buggy = FALSE
SPECIFICATION Spec
INVARIANT TypeOK
INVARIANT MutexOK
PROPERTY JoinCompletes
