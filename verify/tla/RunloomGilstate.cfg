\* Correct (the fix, commit c28e5ca): each hub deletes its OWN tstate on its own
\* thread.  The pystate.c:345 assert never fires and the gilstate binding stays
\* consistent.
CONSTANTS
    Hubs = {h1, h2}
    DeleteOnOwner = TRUE
SPECIFICATION Spec
INVARIANT TypeOK
INVARIANT GilstateContract
INVARIANT GilBindingConsistent
