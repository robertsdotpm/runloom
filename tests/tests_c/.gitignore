# Compiled bench/test binaries (paths are relative to this dir).
bench_mn
bench_mn-asan
bench_mn-tsan
bench_mn-ubsan
bench_server_runloom
bench_server_runloom-asan
bench_server_runloom-tsan
bench_server_runloom-ubsan
# Compiled standalone test binaries (built from test_*.c by the run_*.sh
# scripts; the .c / .sh sources stay tracked).
test_stall_steal
test_stall_pool
test_preempt
test_blockpool
test_cldeque
test_cldeque_lifo
# Sanitizer variants of any bench/test binary.
*-asan
*-tsan
*-ubsan
# Run logs written by the run_*.sh harness scripts.
*.log
