\* Correct: the wake_state machine maintains the refcount ledger -- rc always
\* equals the scheduler ref plus the (0 or 1) outstanding global-runq queue ref,
\* the g is freed exactly once (rc->0), and never while a queue entry could resume it.
CONSTANTS Buggy = FALSE
SPECIFICATION Spec
INVARIANT TypeOK
INVARIANT Ledger
INVARIANT RcNonNeg
INVARIANT FreedConsistent
