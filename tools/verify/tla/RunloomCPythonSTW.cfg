\* Correct protocol: re-attach only via the proper gate (Bypass=FALSE).
\* STWExclusive + RequesterAttached must hold.
CONSTANTS
    Hubs = {h1, h2, h3}
    NoHub = NoHub
    External = External
    Bypass = FALSE
    BuggyBlock = FALSE
    MaxStops = 2
SPECIFICATION Spec
INVARIANT TypeOK
INVARIANT STWExclusive
INVARIANT RequesterAttached
