\* Correct: wake interrupt covers the whole idle window AND every external wake is
\* home-routed.  NoHang (every g eventually done -- across the channel + external
\* seams) must hold.
CONSTANT BuggyQuiesce = FALSE
CONSTANT BuggyRoute = FALSE
SPECIFICATION Spec
INVARIANT TypeOK
INVARIANT RunConsistent
INVARIANT NoLostWake
PROPERTY NoHang
