\* Negative control (the BUG #10 regression): mn_fini signals WITHOUT idle_lock, so
\* the signal can fire while the hub has checked stopping==FALSE but is not yet
\* waiting -> the signal is lost -> the hub waits forever -> pthread_join hangs.
CONSTANTS Buggy = TRUE
SPECIFICATION Spec
INVARIANT TypeOK
INVARIANT MutexOK
PROPERTY JoinCompletes
